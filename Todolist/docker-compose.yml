version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3002:8080"
    environment:
      - ConnectionStrings__DefaultConnection=Server=host.docker.internal;Database=learn;TrustServerCertificate=True;User Id=sa;Password=P@ssw0rd;
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped
